{
    "build": {
        "dockerfile": "./Dockerfile",
        "context": ".",
        "args": {
            "VARIANT": "${localEnv:VARIANT:main}",
            "REGISTRY" : "${localEnv:REGISTRY:ghcr.io}",
            "nrf_toolchain_version": "v2.5.0",
            "sdk_nrf_branch": "v2.5-branch"
        }
    },
    "remoteUser": "vscode"
}
