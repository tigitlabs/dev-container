{
    "build": {
        "dockerfile": "./Dockerfile",
        "context": ".",
        "args": {
            "VARIANT": "${localEnv:VARIANT}",
            "nrf_toolchain_version": "v2.5.0"
        }
    },
    "remoteUser": "vscode"
}
