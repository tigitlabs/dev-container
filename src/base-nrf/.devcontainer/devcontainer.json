{
    "build": {
        "dockerfile": "./Dockerfile",
        "context": ".",
        "args": {
            "BASE_IMAGE": "${localEnv:BASE_IMAGE:ghcr.io/tigitlabs/dev-container/base-ubuntu}",
            "TAG": "${localEnv:TAG:main}",
            "nrf_toolchain_version": "v2.5.0",
            "sdk_nrf_branch": "v2.5-branch"
        }
    },
    "remoteUser": "vscode"
}
